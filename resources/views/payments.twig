{% extends "layouts/app.twig" %}

{% block content %}

<h2 class="font-bold text-xl text-gray-800 my-6 text-center">{{ payments.count() }} people paid 99 cents</h2>
<ul class="flex flex-col p-4">
    {% for payment in payments %}
    <li class="border-2 border-gray-400 flex flex-row mb-2">
        <div class="select-none cursor-pointer bg-gray-200 rounded-md flex flex-1 items-center p-4  transition duration-500 ease-in-out transform hover:-translate-y-1 hover:shadow-lg">
            <div class="flex flex-col rounded-md w-10 h-10 bg-gray-300 justify-center items-center mr-4">💧</div>
            <div class="flex-1 pl-1 mr-16">
                <div class="font-medium">{{ payment.name }}</div>
            </div>
            <div class="text-gray-600 text-xs">{{ payment.created_at }}</div>
        </div>
    </li>
    {% endfor %}
</ul>

{% endblock %}